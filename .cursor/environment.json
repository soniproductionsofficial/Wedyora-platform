{
  "name": "Wedyora Platform",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "api",
      "command": "cd backend && DEMO_MODE=true MOCK_PAYMENTS=true npm run dev",
      "description": "Express marketplace API (demo mode, in-memory data) on http://localhost:4000"
    },
    {
      "name": "web",
      "command": "cd frontend && npm run dev -- --host",
      "description": "Vite marketplace UI on http://localhost:5173 (proxies /api and /socket.io to :4000)"
    },
    {
      "name": "next",
      "command": "npm run dev",
      "description": "Next.js app (marketing + booking/vendor/admin) on http://localhost:3000"
    }
  ],
  "ports": [
    { "name": "next", "port": 3000 },
    { "name": "api", "port": 4000 },
    { "name": "web", "port": 5173 }
  ]
}
