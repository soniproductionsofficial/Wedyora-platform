import type { CSSProperties } from "react";

/** Gold-tinted sweep-line style for use on dark/chrome hero bands. */
export const GOLD_SWEEP_STYLE: CSSProperties = {
  "--sweep-color-1": "rgba(255, 255, 255, 0.7)",
  "--sweep-color-2": "var(--brand-gold-bright)",
} as CSSProperties;
